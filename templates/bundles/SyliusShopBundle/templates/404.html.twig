{% extends '@SyliusShop/layout.html.twig' %}

{% block title %}{{ 'general.404.subtext_vw_html' | t }}{% endblock %}

{% block content %}
    <div class="t-404">
        <div class="site-wrap">
            {% if 'vw' in settings.theme %}
                <h1>{{ 'general.404.title_vw' | t }}</h1>
                <p>{{ 'general.404.subtext_vw_html' | t }}</p>
                <ul>
                    <li><a href="/">{{ 'general.404.homepage' | t }}</a></li>
                </ul>
            {% else %}
                <img src="{{ 'img/404-'|append(settings.theme)|append('.svg') | asset_url }}" class="t-404__image"/>
                <h1>{{ 'general.404.title' | t }}</h1>
                <p>{{ 'general.404.subtext_html' | t }}</p>
                <a class="button button--full" href="/">{{ 'general.404.cta' | t }}</a>
            {% endif %}
        </div>
    </div>
{% endblock %}
